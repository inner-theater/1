# 2026-06-18 工作日志

## 内心剧场 — 修复与功能增强

### 1. 朋友灵魂拷问室：塔罗牌禁止重复抽取
- 新增 `hasDrawnCard` 状态，抽取后按钮变为 "已抽取塔罗牌" 并禁用
- 重新回答/换个问题时重置状态

### 2. 决策日记：AI 模型选项补全
- 模型选择器增加全部 7 个模型：
  - deepseek-v4-pro、deepseek-v4-flash、kimi-k2.5、kimi-k2.6、MiniMax-M2.1、qwen-turbo、qwen-plus

### 3. 人生决策博物馆：公共可见 + 每日限赞
- 数据库：
  - 新建 `museum_items` 表（RLS: 任何人可读，登录用户可发布）
  - 新建 `museum_likes` 表（UNIQUE(user_id, item_id, liked_at) 保证每日限一次）
  - 新建 `increment_museum_like` RPC 函数（原子递增 likes 计数）
- 前端：
  - 从 Supabase 读取所有人的展品
  - 发布需登录
  - 点赞每日每展品限一次，已赞显示 "❤️ 已赞赏" 无法再点
  - 点赞计数实时更新
- 部署：Supabase 迁移已推送，前端已推 GitHub

### 4. 修复 AI 生成题目每次相同的问题
- Temperature 0.9 → 1.05
- 每次请求生成随机种子（shuffleSeed）破坏确定性
- 14 个塔罗牌原型中每次随机选 5 个给 AI 参考（而非固定 10 个）
- Prompt 明确要求「每次生成的题目必须不同，从不同角度切入」
- Edge Function 已部署

### 5. 增强朋友灵魂拷问室深度解读
- 问题：之前传给 AI 的只有 "Q1: 选了A; Q2: 选了B..."，AI 不知道题目内容和选项文本，只能泛泛而谈
- 修复：传入完整 Q&A（每题的问题文本 + 用户选的选项文本）+ 塔罗牌名称/寓意
- Edge Function 新增3步分析：答题目分析心理倾向（找矛盾一致性）→ 塔罗牌呼应 → 具体行动建议
